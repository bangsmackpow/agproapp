# Copy to `.dev.vars` for local development: `cp .dev.vars.example .dev.vars`
# `.dev.vars` is gitignored. Never commit real secrets.

ENVIRONMENT=development

# ── Auth / sessions ──────────────────────────────────────────────────────────
# 32+ byte random value used to sign session cookies and CSRF tokens.
# Generate with: node -e "console.log(crypto.randomBytes(48).toString('base64url'))"
SESSION_SECRET=replace-me-with-a-long-random-value

# ── Drizzle Kit remote access (optional, for db:studio / push) ───────────────
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_DATABASE_ID=
CLOUDFLARE_D1_TOKEN=

# ── Outbound invoice delivery (Phase 4) ──────────────────────────────────────
# MAIL_FROM=invoices@agpro.example
# MAIL_PROVIDER_API_KEY=
